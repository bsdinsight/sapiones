# Sapiones backup — copy thành deploy/backup.env (GITIGNORE). Tất cả TÙY CHỌN.
# BACKUP_DIR=/root/backups
# KEEP_DAYS=7
#
# Đẩy offsite — KHUYẾN NGHỊ MẠNH (VPS chết thì backup cục bộ mất theo).
# Cấu hình rclone remote (xem deploy/BACKUP.md) rồi bỏ comment:
# RCLONE_REMOTE=r2:sapiones-backups
